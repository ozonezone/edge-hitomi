import {Gallery, JsonObject, Tag} from "../type";
import {fetch} from "../utility";
import {RAW_GALLERY_KEYS} from "../constant";
import {edgeFetch} from "./edgeFetch";

export function getGallery(id: number): Promise<Gallery> {
	return;
}
